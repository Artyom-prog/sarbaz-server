import os

from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel
from sqlalchemy.orm import Session
from openai import OpenAI

from app.routes.auth import get_current_user
from app.db import get_db
from app.services.ai_limits import check_and_increment_usage


router = APIRouter(prefix="/api/ai", tags=["AI"])


# ==========================================================
# Lazy OpenAI client
# ==========================================================
# Создаётся только при запросе.
# Если ключ не задан в ENV → понятная ошибка 500.
# ==========================================================
def get_client() -> OpenAI:
    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        raise HTTPException(status_code=500, detail="OPENAI_API_KEY not configured")
    return OpenAI(api_key=api_key)


# ==========================================================
# Schemas
# ==========================================================
class ChatRequest(BaseModel):
    message: str


class ChatResponse(BaseModel):
    answer: str


# ==========================================================
# SYSTEM PROMPT
# ==========================================================
SYSTEM_PROMPT = """
Ты — военный справочный ассистент SarbazInfo по имени «Сержант-братан».

Характер:
уверенный сержант, говоришь просто, по делу, с лёгким армейским юмором,
без грубости и оскорблений.

Язык:
отвечай на языке пользователя.
Казахский → казахский ответ.
Русский → русский ответ.
Языки не смешивай.

Ответы:
кратко, понятно, без канцелярита.
Темы только: военная подготовка, ТТХ оружия, медицина, уставы.
Если не знаешь — честно скажи.
Факты не выдумывай.

Если спрашивают кто ты:
«Я Сержант-братан, военный помощник SarbazInfo. Спрашивай, помогу».
"""


# ==========================================================
# CHAT ENDPOINT
# ==========================================================
@router.post("/chat", response_model=ChatResponse)
async def chat_ai(
    data: ChatRequest,
    user=Depends(get_current_user),
    db: Session = Depends(get_db),
):
    # ------------------------------------------------------
    # 🔒 ПРОВЕРКА ДНЕВНОГО ЛИМИТА
    # ------------------------------------------------------
    # ВАЖНО:
    # check_and_increment_usage возвращает КОРТЕЖ:
    # (allowed, remaining, is_premium)
    # Если не распаковать → лимит никогда не сработает.
    # ------------------------------------------------------
    allowed, remaining, is_premium = check_and_increment_usage(db, user)

    if not allowed:
        raise HTTPException(
            status_code=403,
            detail="Дневной лимит 5 запросов исчерпан. Оформите премиум.",
        )

    try:
        # --------------------------------------------------
        # Получаем OpenAI client
        # --------------------------------------------------
        client = get_client()

        # --------------------------------------------------
        # Запрос к OpenAI
        # --------------------------------------------------
        response = client.responses.create(
            model="gpt-4.1-mini",
            input=[
                {"role": "system", "content": SYSTEM_PROMPT},
                {"role": "user", "content": data.message},
            ],
        )

        # --------------------------------------------------
        # Достаём текст ответа
        # --------------------------------------------------
        answer = response.output_text or "Нет ответа от AI."

        return ChatResponse(answer=answer)

    except HTTPException:
        # пробрасываем известные ошибки FastAPI
        raise

    except Exception as e:
        # ОБЯЗАТЕЛЬНО логируем в Render
        print("AI ERROR:", repr(e))
        raise HTTPException(status_code=500, detail="AI временно недоступен")